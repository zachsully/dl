codata Stream a
  { Head : Stream a -> a
  , Tail : Stream a -> Stream a }

(cocase { # succ ->
	  fix s in
	    cocase
	      { Head # -> 0
	      , Tail # -> succ (Head s) }
	}
)
(cocase { # x -> x + 1})
